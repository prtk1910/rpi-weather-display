# Copy values into /etc/weather-display/environment via scripts/install.sh.
# Never commit the real PIN.
WEATHER_DISPLAY_PIN=choose-a-pin
# WEATHER_DISPLAY_COOKIE_SECURE=1  # only when serving settings through HTTPS
